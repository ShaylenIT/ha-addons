# Contributing to HA Retention Manager

Thanks for your interest in contributing. A short checklist:

- Open an issue describing the change before coding.
- Create a feature branch named `feature/xxx` or `fix/xxx`.
- Keep changes focused and add tests for new behavior.
- Follow Home Assistant integration guidelines.

For development, install requirements with:

```powershell
python -m pip install -r requirements.txt
```
